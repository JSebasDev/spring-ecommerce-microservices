server:
  port: 8082

spring:
  application:
    name: product-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product_db
      username: root
      password: root
      authentication-database: admin